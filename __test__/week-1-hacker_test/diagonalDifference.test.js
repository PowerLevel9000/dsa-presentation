const diagonalDifference = require('../../week-1-hacker/diagonalDifference');

describe('diagonalDifference', () => {
    test('should return 15', () => {
        const arr = [[11, 2, 4], [4, 5, 6], [10, 8, -12]];
        const result = 15;
        expect(diagonalDifference(arr)).toEqual(result);
    });

    test('should return 15', () => {
        const arr = [[11, 2, 4], [4, 5, 6], [10, 8, -12]];
        const result = 15;
        expect(diagonalDifference(arr)).toEqual(result);
    });

    test('should return 15', () => {
        const arr = [[11, 2, 4], [4, 5, 6], [10, 8, -12]];
        const result = 15;
        expect(diagonalDifference(arr)).toEqual(result);
    });

    test('should return 15', () => {
        const arr = [[11, 2, 4], [4, 5, 6], [10, 8, -12]];
        const result = 15;
        expect(diagonalDifference(arr)).toEqual(result);
    });

    test('should return 15', () => {
        const arr = [[11, 2, 4], [4, 5, 6], [10, 8, -12]];
        const result = 15;
        expect(diagonalDifference(arr)).toEqual(result);
    });

    test('should return 15', () => {
        const arr = [[11, 2, 4], [4, 5, 6], [10, 8, -12]];
        const result = 15;
        expect(diagonalDifference(arr)).toEqual(result);
    });

    test('should return 15', () => {
        const arr = [[11, 2, 4], [4, 5, 6], [10, 8, -12]];
        const result = 15;
        expect(diagonalDifference(arr)).toEqual(result);
    });

    test('should return 15', () => {
        const arr = [[11, 2, 4], [4, 5, 6], [10, 8, -12]];
        const result = 15;
        expect(diagonalDifference(arr)).toEqual(result);
    });
});